services:
  - type: web
    name: chc-crm
    runtime: docker
    dockerfilePath: ./Dockerfile
    repo: https://github.com/Easeaiworks/REFINISH-AI-CRM.git
    branch: main
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
    healthCheckPath: /api/health
